# 07 — Public Repository

## Intended public root

Contents listed in `PUBLIC_TREE.md`.

## Must ship

Runtime, `assets/genesis`, at least one demo update under `assets/updates`, `ui/`, `install.sh`, INSTALL/CHANGELOG/RELEASE_NOTES/CERTIFIED/VERSION.

## Must not require

Founder `.env`, monorepo `scripts/`, cluster Node-01, Website sibling repo.

## Git hygiene

`.citizen/`, `lab/`, `**/publisher.secret` gitignored.
