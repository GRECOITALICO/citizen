# Timeline

This is the official life spine of the Citizen Seed era, written for humans.

Each node names what was true: Manifest posture, Runtime, Evidence, Assets, and the change that node represents.

```
Birth
  │
  0.1
  │
  0.2
  │
  …
```

---

## Birth

| Field | Observed content |
|-------|------------------|
| **Meaning** | Irreversible genesis of one Citizen home |
| **Runtime** | `0.1.0` |
| **Manifest** | First sealed genesis Manifest (citizen version seed baseline before sync) |
| **Evidence** | PreBirth → … → `BIRTH_COMPLETE` |
| **Assets** | Genesis four: `citizen_ui_shell`, `docs_index`, `status_seed`, `website_shell` |
| **Changes** | Identity minted; Bootstrap disarmed; memory planes opened |

Inner spine (also recorded in the living Timeline plane):

PreBirth → Bootstrap → Identity → Evidence → Telemetry → Manifest → Projection → Alive

---

## 0.1 — Seed Runtime line

| Field | Observed content |
|-------|------------------|
| **Meaning** | First public Runtime generation of the Citizen Seed |
| **Runtime** | `0.1.0` |
| **Manifest** | Compatibility `seed-2026.1` |
| **Evidence** | Boot marks; Observatory life |
| **Assets** | Genesis set projected |
| **Changes** | Citizen can boot and be witnessed without Sync |

Version label **0.1** here means the Runtime/compatibility generation, not a marketing product number.

---

## 0.2 — First Sync (`1.0.1-seed`)

| Field | Observed content |
|-------|------------------|
| **Meaning** | First Evolution of carried Assets under the same genome |
| **Runtime** | Still `0.1.0` |
| **Manifest** | `citizen_version` **1.0.1-seed**; release `2026-08-01T12:00:00Z`; new `asset_version` hash |
| **Evidence** | `UPDATE_STATE_*` through Updated; Journal/Timeline *Sync* |
| **Assets** | Same Asset ids; updated content hashes / docs Asset version as packaged |
| **Changes** | Manifest history retains prior release; Identity unchanged |

On the living Timeline this appears as node *Sync* after *Alive*.

---

## Next nodes

Future Syncs / Runtime bumps append here as **0.3**, **0.4**, … each with Manifest, Runtime, Evidence, Assets, Changes. Prior nodes stay.

Until a new package or Runtime is observed, no invented nodes are added.
