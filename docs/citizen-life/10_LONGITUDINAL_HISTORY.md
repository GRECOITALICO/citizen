# Longitudinal History

This document never restarts. It only grows.

Earlier chapters are never deleted and never rewritten. New eras of Citizen life are added as new chapters below the last line of history.

Editors: append only. If a fact was wrong, add a later chapter that corrects with evidence — do not silently alter the past.

---

## Chapter I — Seed Birth Era (2026-08)

**Title.** The Citizen Seed becomes a living organism.

**What occurred.**  
A public-style tree `citizen-seed/` gained `./install.sh` Birth: Identity, Manifest, Evidence, Telemetry, Journal, Timeline, Projection, Observatory UI. Bootstrap disarmed after Alive. First Sync applied package `1.0.1-seed` while Runtime stayed `0.1.0`. Birth Packages were exported under `lab/exports/`. Destroy → Birth cycles produced new `citizen_id`s with preserved prior packages.

**What changed.**  
From “empty home” to “alive Citizen”; from genesis Manifest to post-sync Manifest; documentation opened this official Citizen Life section.

**What remained.**  
Asset-first law; no LLM required for measured life planes; append-only memory planes.

**Evidence.**  
Living planes under `CITIZEN_HOME`; Timeline spine PreBirth→…→Alive→Sync; `lab-report` verdict `ALL_MEASURED_LIFE_OUTSIDE_LLM` (8/8 planes, `llm_free: true`, measured 2026-08-01T05:33:48Z); multiple `lab/exports/birth_*.tar.gz` with distinct ids and hashes. Research serials INT-CITIZEN-SEED-IMPLEMENTATION-001 and INT-CITIZEN-BIRTH-LAB-001 prepared the ground; this chapter is the official life entry.

---

## Chapter II — *(next era)*

*Reserved for the next observed era. Append below this marker when evidence exists. Do not replace Chapter I.*
