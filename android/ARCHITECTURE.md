# Android Surface Architecture

Android = Citizen Surface. Citizen Core remains the owner of canonical state.

## Architecture

`CITIZEN CORE -> CITIZEN PROTOCOL -> ANDROID SURFACE`

The Android app serves merely as an interface (Connect, Status, Conversation, Update). It must not dictate identity, state, or permissions over the Citizen entity.
