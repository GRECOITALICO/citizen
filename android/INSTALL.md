# Installation

Android = Citizen Surface. Citizen Core remains the owner of canonical state.

To build and install the V0 Android Surface:
1. Compile the APK using Gradle (`./gradlew assembleDebug`).
2. Deploy to a test device or emulator using ADB.
3. The surface will connect to the Citizen Core via the established Citizen Protocol.
