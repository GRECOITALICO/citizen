# seed_package/ — legacy duplicate

**Not required** for Citizen Seed 0.1 Birth.

Canonical planes:

- `assets/genesis/`  
- `assets/updates/`  
- `assets/publisher.secret.example`  

This folder remains only as historical packaging residue. Third-party installs must not depend on it. Future cleanups may remove it in a **0.2** tree without rewriting 0.1 release notes.
